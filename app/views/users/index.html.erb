<% provide(:title, 'All users') %>
<h1>All users</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      name: <%= link_to user.name, user %><br>
      email: <%= user.email %><br>
      age: <%= user.age %><br>
      gender: <%= user.gender %><br>
      <%= link_to "削除", users_path, method: :delete, data: { turbo_method: :delete } if user == current_user %><br>
    </li>
  <% end %>
</ul>
